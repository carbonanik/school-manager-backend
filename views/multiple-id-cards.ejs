<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student ID Cards</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            /* height: 100vh; */
            background-color: #f4f4f4;
        }
        .page {
            width: 210mm;
            height: 297mm;
            padding: 10mm;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 10mm;
            box-sizing: border-box;
        }
        .id-card img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin-bottom: 10px;
            border: 2px solid #007bff;
        }
    </style>
</head>
<body>
    <div class="page">
        <%- include('id-card', { student: student1 }) %>
        <%- include('id-card', { student: student2 }) %>
        <%- include('id-card', { student: student3 }) %>
        <%- include('id-card', { student: student4 }) %>
        <%- include('id-card', { student: student5 }) %>
        <%- include('id-card', { student: student6 }) %>
    </div>
</body>
</html>
