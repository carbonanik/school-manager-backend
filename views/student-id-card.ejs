<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student ID Card</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }

        .id-card {
            width: 300px;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .id-card-image {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin-bottom: 10px;
            border: 2px solid #007bff;
            display: block;
            margin: 0 auto;
        }

        .barcode-image {
            height: 80px;
            margin-bottom: 10px;
            display: block;
            margin: 0 auto;
        }

        .id-card h2 {
            margin: 5px 0 5px;
            color: #333;
        }

        .id-card p {
            margin: 5px 0;
            color: #555;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="id-card">
        <img class="id-card-image" src="<%= student.image %>" alt="Student Photo">
        <h2>
            <%= student.name %>
        </h2>
        <p><strong>ID:</strong>
            <%= student.id %>
        </p>
        <p><strong>Class:</strong>
            <%= student.class %>
        </p>
        <p><strong>School:</strong>
            <%= student.school %>
        </p>
        <div class="barcode">
            <img class="barcode-image" src="<%= student.barcode %>" alt="Barcode">
        </div>
    </div>
</body>

</html>